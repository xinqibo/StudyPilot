[project]
name = "project"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "openai>=2.0.0",
    "pydantic>=2.13.4",
    "python-dotenv>=1.0.0",
    "langgraph",
]
